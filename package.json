{
  "name": "crox-monorepo",
  "private": true,
  "version": "0.2.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "cd client && npm install && npm run build",
    "preview": "cd client && npm run preview"
  }
}
